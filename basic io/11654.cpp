#include <bits/stdc++.h>
using namespace std;

char c;
int val;

int main(){
    
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    cin >> c; //문자열 받기
    val = c; //아스키코드값을 정수형 val 변수에 
    
    cout << val;
}
